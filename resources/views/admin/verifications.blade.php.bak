<!-- Backup of original admin verifications blade -->
<div>
    <header class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-10">
        <div class="px-8 py-6">
            <h1 class="text-2xl font-bold text-gray-900">Verifikasi KTP (Sederhana)</h1>
            <p class="text-sm text-gray-500 mt-1">Halaman verifikasi yang dibangun ulang dari nol.</p>
        </div>
    </header>

    <div class="p-8">
        @if (session()->has('message'))
            <div class="mb-4 text-sm text-white bg-green-600 px-3 py-2 rounded">{{ session('message') }}</div>
        @endif

        <div class="bg-white rounded-lg shadow overflow-hidden">
            <table class="w-full text-sm">
                <thead class="bg-gray-100">
                    <tr>
                        <th class="p-3 text-left">ID</th>
                        <th class="p-3 text-left">Nama</th>
                        <th class="p-3 text-left">Email</th>
                        <th class="p-3 text-left">Status</th>
                        <th class="p-3 text-center">Aksi</th>
                    </tr>
                </thead>
                <tbody>
                    @forelse($verifications as $v)
                        <tr class="border-t hover:bg-gray-50">
                            <td class="p-3">#{{ $v->id }}</td>
                            <td class="p-3">{{ $v->full_name ?? ($v->name ?? '-') }}</td>
                            <td class="p-3">{{ $v->email ?? '-' }}</td>
                            <td class="p-3">{{ ucfirst($v->status ?? 'pending') }}</td>
                            <td class="p-3 text-center">
                                <button type="button" wire:click="viewKtp({{ $v->id }})" class="px-3 py-1 bg-blue-600 text-white rounded">Lihat</button>
                                <button type="button" wire:click="approveKtp({{ $v->id }})" class="px-3 py-1 bg-green-600 text-white rounded ml-2">Verifikasi</button>
                                <button type="button" wire:click="rejectKtp({{ $v->id }})" class="px-3 py-1 bg-red-600 text-white rounded ml-2">Tolak</button>
                            </td>
                        </tr>
                    @empty
                        <tr>
                            <td colspan="5" class="p-6 text-center text-gray-500">Tidak ada data verifikasi.</td>
                        </tr>
                    @endforelse
                </tbody>
            </table>

            <div class="p-4 bg-gray-50">
                {{ $verifications->links() }}
            </div>
        </div>

        @if($showModal && $selected)
            <div class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-40">
                <div class="bg-white rounded-lg w-full max-w-2xl p-6">
                    <div class="flex justify-between items-center mb-4">
                        <h3 class="font-semibold">Detail Registrasi #{{ $selected->id }}</h3>
                        <button type="button" wire:click="closeModal" class="text-gray-600">Tutup</button>
                    </div>

                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <div class="text-xs text-gray-500">Nama</div>
                            <div class="font-medium">{{ $selected->full_name ?? ($selected->name ?? '-') }}</div>
                        </div>
                        <div>
                            <div class="text-xs text-gray-500">Email</div>
                            <div class="font-medium">{{ $selected->email ?? '-' }}</div>
                        </div>
                    </div>

                    <div class="mt-6 flex justify-end space-x-3">
                        <button type="button" wire:click="approveKtp({{ $selected->id }})" class="px-4 py-2 bg-green-600 text-white rounded">Verifikasi</button>
                        <button type="button" wire:click="rejectKtp({{ $selected->id }})" class="px-4 py-2 bg-red-600 text-white rounded">Tolak</button>
                    </div>
                </div>
            </div>
        @endif
    </div>
</div>
